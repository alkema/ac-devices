<%= form_with(model: api_v1_device, local: true) do |form| %>
  <% if api_v1_device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_device.errors.count, "error") %> prohibited this api_v1_device from being saved:</h2>

      <ul>
        <% api_v1_device.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
